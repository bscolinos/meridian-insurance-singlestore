DEMO_NAME=meridian-insurance
# --- SingleStore workspace (fill from your provisioned workspace) ---
SINGLESTORE_HOST=
SINGLESTORE_PORT=3306
SINGLESTORE_USER=admin
SINGLESTORE_PASSWORD=
SINGLESTORE_PROJECT_ID=
# --- Aura Analyst — server-side only, from Portal > Analyst > domain > API Keys.
# Crawl a `meridian_intel` domain on this workspace, then paste endpoint + key.
ANALYST_API_URL=https://apps.<region>.cloud.singlestore.com/v1/organizations/<org>/projects/<proj>/analyst/chat
ANALYST_API_KEY=
